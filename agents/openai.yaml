interface:
  display_name: "OpenClaw 多机器人配置"
  short_description: "解释并安全配置 OpenClaw 多机器人和多 Agent 路由"
  default_prompt: "Use $openclaw-multi-bot-config to explain OpenClaw multi-bot routing choices or to preview and safely apply changes to agents, channels, bindings, and session.dmScope in openclaw.json."

policy:
  allow_implicit_invocation: true
